import React from 'react';
import Menu from "@/app/components/menu/Menu";

const Header = () => {
    return (
        <header>
            <h1>MovieLand</h1>
            <Menu/>
        </header>
    );
};

export default Header;